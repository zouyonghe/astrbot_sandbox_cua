# astrbot_sandbox_cua

CUA sandbox runtime provider plugin for AstrBot.

This plugin registers the `cua` sandbox provider through AstrBot core's generic
sandbox provider API. Install it under `data/plugins/astrbot_sandbox_cua` or via
the AstrBot plugin manager.

## Capabilities

- shell
- python
- filesystem
- screenshot
- mouse
- keyboard
